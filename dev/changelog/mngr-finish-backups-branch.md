Pinned restic to 0.18.1 in the workspace image (`scripts/setup_system.sh`): a sha256-verified download installed at `/usr/local/bin` that shadows the Debian bookworm apt package's 0.14. The minds in-place backup restore requires `restic restore --delete` / `--overwrite if-changed` (restic >= 0.17); baking the pinned build into new images keeps the restore script's own download-and-install fallback dormant, and the hourly host-backup service runs the same restic version minds bundles on the desktop side.
