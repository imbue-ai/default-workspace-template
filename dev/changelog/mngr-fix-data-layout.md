Debian trixie base image with snapshot-pinned apt sources (`scripts/write_apt_sources.sh` + the committed `.mngr/apt-snapshot-timestamp`), the /home/user layout cutover (usermod, seed home skeleton, cache redirection, tmpfs /tmp, provider layout knobs in `.mngr/settings.toml`), env.d units replacing deferred_install.sh, and a specs note (`specs/inspiration-environment-manifests.md`) recording the future inspiration environment-manifest direction.
