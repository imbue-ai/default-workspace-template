# Frontend choices

Use these default choices unless the user has requested something different, or the specific task clearly requires a different choice.

## daisyUI
Use daisyUI default components and styling whenever possible. You can invoke the `daisyui` skill to get a list of available components and installation/integration instructions.