The build-app scaffold's pages carry a module script that connects the page to the workspace shell, reports its location on the handshake, and installs the element context menu, in place of the old location beacon; the runner serves `/_static/app_contract.js` and `/_static/context_menu.js` from its own origin. The skill says to keep both on every page and to give list rows and controls stable ids or data attributes so a reference resolves to one thing.
