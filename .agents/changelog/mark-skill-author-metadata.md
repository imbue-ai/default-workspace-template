Every `SKILL.md` under `.agents` now carries `metadata.author: imbue` in its frontmatter -- all 35 skills plus the shared `harden-worker` sub-skill. `metadata` is the agentskills.io spec's optional string-to-string map for properties outside the spec proper, so this is where authorship belongs; the three skills that already had a `metadata` block gain the key alongside their existing entries instead of a second block. Nothing else in the frontmatter or bodies changed.
