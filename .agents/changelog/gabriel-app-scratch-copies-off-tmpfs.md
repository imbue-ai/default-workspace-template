`serve_isolated_instance.py up --copy` (which app previews use for their copy of an app's data) now checks that the copy fits before making it. A copy that would leave less than 2 GB free on its disk is refused before anything is written, failing the boot with a message saying what to do instead, and a copy that fails part way is removed rather than left holding the space.
