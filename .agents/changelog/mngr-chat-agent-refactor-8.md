The successor's first message after a handoff now carries the predecessor's summary in full, with the path it is kept at, rather than only the path, when the summary is 64 KB or under; a larger one is pointed at, and the successor is told to read it first. The `continue-chat` reference takes the summary's text, and the `handoff-summary` skill tells the retiring agent that what it writes goes into the successor's first message, or is pointed at when it is over that size.
