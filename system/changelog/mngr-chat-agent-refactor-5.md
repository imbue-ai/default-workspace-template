Phase 5 of the chat-agent split (`docs/system/blueprint/chat-agent-split/plan-chat-agent-split.md`): the user's side of the handoff landed in the chat app. The spec's section 5.1 records what the page does (the pending lane, "Switch and send", the confirm, the held messages with the phase as their caption, "Cancel switch", the failed page, the 409 details shown as is), section 4.6 the two fields the snapshot's `handoff` object gained, and the Phase 5 entry the tests that stand for the exit check while CI lacks an OpenRouter key.
