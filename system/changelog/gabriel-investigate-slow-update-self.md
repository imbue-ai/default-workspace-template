The app manifest docs (`system/apps/README.md`, the workspace app model's `contracts.md`) now say an app's `[wiring] programs` and the blocks its footprint carries are declared under `system/supervisord.conf.d/`, where the template keeps every program, rather than in `system/supervisord.conf`, which the footprint library now reads the drop-ins of. `system/test_app_manifests.py` now computes every built-in app's footprint against the real tree and checks it carries the drop-in that runs each of the app's programs, and `system/test_supervisord_layout.py` pins the library as one more reader of the include glob.
