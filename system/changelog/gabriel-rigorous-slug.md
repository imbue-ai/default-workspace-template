The rule that marks an agent's shell commands as the first thing to stop when the workspace runs low on memory now comes from one shared place, rather than each caller spelling it out. Behaviour is unchanged -- the same commands are marked the same way -- but the workspace interface's own repair-rebuild now uses that same rule instead of its own copy, so a future correction reaches both.
