`docs/system/specs/worker-partial-artifacts/` holds the design for worker milestones: `proposal.md` (the rationale and the alternatives considered -- git tags, reusing the single-slot `report.md`, merging the branch tip) and `concise.md` (the spec: the milestone report format, how the lead's poll returns it, the provisional-merge and rollback rules, and the per-file changes). The mechanism itself lives under `.agents/` and has its own entry there.
