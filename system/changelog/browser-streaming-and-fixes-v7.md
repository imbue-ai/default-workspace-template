The browser (agentic-browser-fleet) service now runs as a supervised program at slightly elevated CPU priority (nice -5) so agent build/test bursts cannot starve the live pixel path, and the base image bakes the small runtime libraries the fleet's pixelflux encoder and audio path load at import (libva, x11-utils, pulseaudio, ffmpeg), so it works from first boot.
