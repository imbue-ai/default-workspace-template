The streamed-browser service runs as a supervised program at slightly elevated CPU priority so agent build/test bursts cannot starve the live pixel path, and the base image now bakes the small runtime libraries (libva, x11-utils) its encoder loads at import, so the service works from first boot. The agentic browser fleet runs at the same elevated priority for the same reason.
