Phase 4 of the chat-agent split (`docs/system/blueprint/chat-agent-split/plan-chat-agent-split.md`): the backend of the handoff landed in the chat app. The spec's handoff sections and its Phase 4 entry now record what landed and where it departs from the sketch (draining is the stop button's interrupt; the archive is one `mngr rename` carrying every label; no addendum to the summary; tk steps carry over instead of being closed; held sends answer 202), and `contracts.md` section 4.3 says stop, start, and rename answer 409 while a chat moves to another agent.

The vendored `tk` (`system/vendor/tk/ticket`) scopes step records by the chat's id (`MINDS_CHAT_ID`) when the chat app set one, falling back to the agent's name as before, so a chat that moves to a new agent keeps its open steps and a renamed agent no longer loses the steps it created after the rename; `system/scripts/agent_open_tickets_reminder.sh`'s note follows.
