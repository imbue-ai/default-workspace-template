`layout.py open` takes `--beside [window]`: bare, it pairs the opened window with
your own chat; with a window argument, with that window. The desktop contract's
`open` row documents the new `beside` argument.
