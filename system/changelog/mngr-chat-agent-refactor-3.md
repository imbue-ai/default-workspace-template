Phase 3 of the chat-agent split (`docs/system/blueprint/chat-agent-split/plan-chat-agent-split.md`): the read side of a chat that has run on several agents landed in the chat app. The spec's Phase 3 section now records what landed and where it departs from the sketch (lazy sequential loading rather than a thread pool; the record is deleted with a destroyed chat; `active_agent` carries no `lane`; the `agent_switch` event type), and `contracts.md` section 4.3 says an archived member of a chat is never an instance and that a chat's delete names every member.
