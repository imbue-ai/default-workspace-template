Added `docs/system/blueprint/desktop-interface/concepts.md`, the concept and naming document for converting the system interface from a tabbed dock to a desktop-style interface (desktops, windows, shortcuts on a surface, a dock with a start button and system tray).
It records what stays from the workspace app model, what each prototype contributes, and the open questions with recommendations that the V1 spec will build on.
No code changes.
