AGENTS.md now tells agents to write workspace file paths in chat replies in code formatting and suggest the File Viewer, rather than linking them: the chat renders only web links and absolute-path download links.
