The app manifest docs (`system/apps/README.md`, the workspace app model's `contracts.md`) now say an app's `[wiring] programs` and the blocks its footprint carries are declared under `system/supervisord.conf.d/`, where the template keeps every program, rather than in `system/supervisord.conf`. `system/test_app_manifests.py` now computes every built-in app's footprint against the real tree and checks that it carries the drop-in running each of the app's programs.
