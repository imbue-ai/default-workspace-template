forward_port.py now refuses a registration that would create a brand-new pickable entry without an icon (--icon/--icon-file, .svg only), so new apps cannot fall back to the generic letter monogram; existing entries re-register untouched. A new --no-icon flag opts out for non-pickable machinery (system_interface, browser, terminal, previews).
