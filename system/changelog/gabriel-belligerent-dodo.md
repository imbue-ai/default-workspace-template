The CLAUDE.md ModuleNotFoundError workaround now gives the correct recovery: `uv sync --all-packages` is the fix in the standard workspace (mngr runs from the root venv, not as a uv-managed tool), and the `uv tool install` variant -- only relevant when `uv tool list` shows the tool -- now points at `system/vendor/mngr/libs/mngr`, the installable package inside the vendored monorepo (installing the vendored root fails with a setuptools flat-layout error).
