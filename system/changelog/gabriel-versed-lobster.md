Hardened the download curls in `system/scripts/setup_system.sh` (and the secret-scanner tarball fetch in `install_secret_scanners.sh`) against release-CDN connection resets: a shared `curl_fetch` helper now retries up to 8 times with `--retry-all-errors` (plain `--retry` does not retry connection resets, which had failed the whole provision with `curl: (56) Connection died` under `set -e`), bounded by `--retry-max-time 300`.
