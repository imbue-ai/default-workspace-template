Re-enabled the `owner-exec` service under supervisord. The earlier spawn error was caused by `owner-exec` not being a dependency of the workspace's root project: a root-closure-scoped `uv sync` (run by the backup restore flow before restarting services) pruned the package from the venv, so `uv run owner-exec` could not find the console script. The package is now a root dependency and the supervisord program block is active again.
