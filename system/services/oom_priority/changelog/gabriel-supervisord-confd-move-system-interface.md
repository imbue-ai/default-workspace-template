Documentation only: the OOM wrapper's module docstring no longer notes that `system_interface` is declared in `system/supervisord.conf` itself. That program moved into its own drop-in, so every command carrying the `oom_tag_service.py` prefix now lives under `system/supervisord.conf.d/`. The band-coverage checks are unaffected -- they already read the config through its `[include]` globs, and still see all thirteen programs.
