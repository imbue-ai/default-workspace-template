`SERVICE_BANDS` gains `agent-observer` at 24, just below the chat: the observer is the writer of the agent lifecycle event file every chat instance follows, so shedding it blinds every chat's agent view at once until supervisord brings it back, while freeing almost nothing; a shed chat app comes back to a stream the observer kept writing. (The built-in service bands are strictly ordered and distinct, so it cannot share the chat's 25.)
