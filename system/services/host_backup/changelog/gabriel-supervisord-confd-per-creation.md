Documentation only: the `[program:host-backup]` block now lives in `system/supervisord.conf.d/host-backup.conf` rather than in the shared `system/supervisord.conf`, and the README says so. The block, its program name, and the `uv run host-backup` entry point are unchanged -- the stable contract minds relies on for backup-service updates still holds, since minds addresses the service as `supervisorctl status|restart host-backup` by name and never by file. Only the file declaring it moved.
