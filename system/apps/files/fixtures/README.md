# Files listing fixtures

Captured from the pinned dufs binary serving an isolated workspace through the
vendored Files assets. `home.json` represents `/data/`; `workspace.json` represents
`/`. Entries are reduced to a single representative directory, with timestamps
removed. The server fields and path conventions are preserved.
