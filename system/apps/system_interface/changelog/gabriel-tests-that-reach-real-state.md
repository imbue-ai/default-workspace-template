The system interface's test suite now carries the same temp-directory guard as the rest of the repo. It roots separately from the repo (its own pytest config), so it would not have inherited the repo-wide fix for a workspace container's `noexec` `/tmp` the day one of its tests started running a stub binary it had written. Nothing in it does today, and its 326 tests are unaffected.
