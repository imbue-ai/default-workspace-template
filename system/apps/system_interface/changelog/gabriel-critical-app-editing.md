`app.toml` gains the shell's `[preview]` table: a preview boots `system-interface --preview --state-dir {copy:state}` over a seeded copy of `data/.state/system_interface`, reads a copied registry through `MINDS_APPS_FILE = {registry}`, and probes `/api/health`.

`system-interface --preview` boots the shell as a read-only preview of a proposed change: over the state directory it is given (a seeded copy) and the registry `MINDS_APPS_FILE` names, it refuses every relay verb, the app-level stop and start, and a create through the op route with a 403 naming the preview, says `is_preview` in its inventory document, marks its page with the `system-interface-preview` meta tag, and raises no staleness banner. The frontend reads the tag and hides the verbs the backend refuses (rename, delete, stop, and start on the tab and rail menus, the New Tab tiles, the picker's rows, and a stopped app's Start); tab, filing, and pinning verbs stay, since they edit the preview's own copy.

The shell raises the update notice the careful flow's apply leaves behind. `shell/update_notice.py` reads and watches `data/.state/update-apply/last-good.json` (the record `update_self.py apply --keep-rollback-point` keeps), pushes every distinct reading over the socket as `update_notice_changed` (seeded on connect), and answers `GET /api/updates/pending` (the notice, or `null`), `POST /api/updates/pending/confirm` (runs the script's `confirm-last`; `204`, `409` without a record, `500` naming a failed script) and `POST /api/updates/pending/rollback` (starts `rollback-last` detached in its own session, logging to `rollback-last.log` beside the record; `202`, `409` while one runs or once the point was taken back); a preview shell answers both verbs with its `403`. The frontend renders the notice as a band above the page in every tab of an app the record names (`UpdateNoticeBand`, inside `IframePanel` in a constant column so the band's coming and going never reloads the frame) and as a top banner for the shell itself (`UpdateNoticeBanner`): "Roll back" asks first, naming the update, the apps, and the programs that restart (and warning when the restart is left to an agent), "Everything seems good" confirms, a running rollback shows its progress with no verbs, and a settled one its outcome with Close. The registry watch and the notice watch share `shell/file_watch.py`. The direct-subprocess ratchet allows the one detached launch.

The README's "Updating the running UI" describes the careful flow the shell's `critical = true` routes every edit through, with `preview_app.py` in place of the removed reveal script and `--keep-rollback-point` on the apply.
