The browser app's tests now run without the ambient agent identity. `fleet.py` reads `MINDS_CHAT_ID` and falls back to `MNGR_AGENT_ID`; one test set only the latter, so it read the runner's real chat id and picked the wrong browser view. Every Minds chat agent runs with both set, so the test passed in CI and failed for every agent -- the combination that teaches agents to ignore a red suite.
