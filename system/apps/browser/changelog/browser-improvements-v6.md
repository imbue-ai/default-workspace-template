The live browser view now streams as real video over WebRTC when possible: smoother motion and far less bandwidth than the frame-by-frame JPEG stream, which stays as the automatic fallback (the viewer silently switches back whenever a peer connection can't be established or drops). Nothing changes in how you drive the browser -- input, tabs, control handoff, and clipboard all work identically on either transport.
