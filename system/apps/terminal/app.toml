name = "terminal"
display_name = "Terminal"
icon = "icon.svg"
instances = true
# The instances API port (served by the terminal app from phase 3 of the
# workspace app model on); ttyd itself stays on 7681.
instances_url = "http://127.0.0.1:7682"
critical = true
priority = "terminal"

[default_shortcut]
action = "new"
mode = "focus"

[[actions]]
id = "new"
label = "New Terminal"
params = [{name = "workdir", label = "Working directory", required = false}]
