The report that `system/scripts/run_in_background.py` sends an agent when its background command finishes shows as a one-line notice ("Background task: <what it was> (finished)"), like claude's own background-task notices, instead of as a message the user seems to have typed. The full report still reaches the agent.
