The workspace's first chat now displays as "Chat 1" -- the same series the New Tab launcher continues ("Chat 2", ...) -- instead of wearing the workspace's name. The mngr-level agent keeps its host-derived name (agent names cannot hold spaces); the bootstrap files "Chat 1" into the machine-wide member-title store at create time, exactly as the launcher titles the chats it creates. First boot only, so existing workspaces keep their current chat name.

The workspace's first chat now carries its human-readable name in mngr itself. The bootstrap creates it as `Chat 1@<host>` rather than under the host's name, so mngr canonicalizes it to the agent name `Chat-1` and keeps "Chat 1" as the agent's `display_name` label -- `mngr list` and anything else reading mngr directly now show the chat's real name instead of the workspace's. The bootstrap still files "Chat 1" into the machine-wide member-title store, because nothing yet lifts the `display_name` label into the serialized agent state the frontend draws tabs from; that write goes away once the label is plumbed through.
