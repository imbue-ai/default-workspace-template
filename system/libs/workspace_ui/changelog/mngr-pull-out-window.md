The shared embed library learns the pull-out set of the minds embed contract (version 6): `pop-out-window`, `pop-out-cancel`, `pop-out-end`, and `detached-windows` (workspace to embedder), and `embedder-capabilities` and `reattach-window` (embedder to workspace), each probed on the vendored snapshot with a literal fallback so a frontend builds against the pinned older contract too.
