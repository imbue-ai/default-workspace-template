The repo-wide `workspace vocabulary in mngr-level code` ratchet (`test_meta_ratchets.py`) is tightened from 333 to 328 recorded matches, and the `minds references in mngr-level code` ratchet from 317 to 314: the imbue_cloud plugin's host docstrings and the adopt-path comments in `create_agent_state` now speak of the lease's agent id rather than a "workspace identity" that minds keeps on the host, and the lower counts lock that in (imbue-ai/mngr-internal#1159).
