Add the minds notifications overhaul blueprint (`blueprint/minds-notifications-overhaul/`). It makes the feed the single pipeline for every notification kind (permission requests, agent messages, system events), fixes the OS-banner focus gate whose signal goes stale while keyboard focus is inside the workspace iframe, adds a template skill for chat agents to notify the user at the end of every turn that did work, with a click-to-chat deep link, drops browser-mode delivery and the osascript/tkinter fallbacks, turns "Couldn't open link" into an in-app toast, and folds in issue #740 (cards flash in every window). The earlier `blueprint/minds-notification-system/` plan now carries a pointer to it.
