The public download link's fallback (`_DEFAULT_TARGET_BY_PLATFORM`, served while the stable manifest cannot be read) moves to the minds 0.6.2 build `260917ohslvgoyj`, matching the stable channel promotion. The drift test that pins the fallback to the stable channel now matches on the version and build id rather than the product name, which ToDesktop renamed from `Minds` to `Mind` in this build.
