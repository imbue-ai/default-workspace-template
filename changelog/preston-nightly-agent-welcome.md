The Caretaker's first-night greeting is now an automatic, fixed welcome -- delivered the same way the main chat's greeting is (a `/caretaker-welcome` message sent at create time), not something the agent improvises while running a routine. So the very first thing you see from the Caretaker is the welcome itself. The nightly routine (checking apps, proposing fixes) only runs on later nights.
