# data/creations/

Stored data for each creation, one folder per creation name. The creation's
code lives at `/home/user/workspace/creations/<name>/`; whatever it saves --
records, caches, snapshots, user content -- goes in `data/creations/<name>/`.
